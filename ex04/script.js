let nome = "";
let sobrenome = "";
while (nome === "") {
    nome = prompt("Digite seu primeiro nome:");
}
while (sobrenome === "") {
    sobrenome = prompt("Digite seu sobrenome:");
}
alert("Olá, " + nome + " " + sobrenome + "!");